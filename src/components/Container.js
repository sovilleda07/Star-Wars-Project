import styled from 'styled-components';

// Componente Container que sólo es Styled Component
const Container = styled.div`
  max-width: 420px;
  margin: 0 auto;
`;

export default Container;
